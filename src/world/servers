#Format in this file, one server per line (max line length is 159, do NOT exceed!):
#'servername' (string)	serverpass (string)	index (int)	flags (string of flag characters)
#Note that the server name MUST be inside single quotes, which allows for use of spaces.
#Names containing 'Relay' (case-insensitive) or 'IRC' (all caps) are specifically for IRC relay usage.
#eg:	'my server'	mypassword	0	C

